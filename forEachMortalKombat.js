const mortalKombat = ["Liu Kang", "Sub-Zero", "Raiden", "Scorpion", "Johnny Cage"];
mortalKombat.forEach(function(elemento, index, array) {
    console.log("Index:" + index, "Elemento:" + elemento, "Array:" + array);
});